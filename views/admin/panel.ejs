<!DOCTYPE html>
<html lang="en">
<head>
<meta charset="UTF-8">
<title>Admin Panel | Lesy Luxury</title>
<link rel="stylesheet" href="/css/style.css">
<style>
body { font-family: Arial; background: #f8f9fa; padding: 20px; }
h1, h2 { text-align: center; }
form { margin: 20px auto; width: 80%; background: #fff; padding: 20px; border-radius: 10px; box-shadow:0 0 10px rgba(0,0,0,0.2);}
form input, form select, form button { width: 90%; padding: 15px; margin: 10px 0; font-size: 18px; border-radius:5px; border:1px solid #ccc;}
form button { background: #28a745; color: #fff; border:none; cursor:pointer;}
form button:hover { background: #218838;}
.product-card { display:inline-block; border:1px solid #ccc; margin:10px; padding:10px; width:220px; background:#fff; border-radius:8px;}
.message-box { border:1px solid #000; padding:10px; margin:10px; border-radius:5px; background:#fff;}
.message-box input { width: 80%; }
.message-box button { width: 15%; background:#007bff; color:#fff; border:none; border-radius:5px; cursor:pointer;}
.message-box button:hover { background:#0056b3;}
</style>
</head>
<body>
<h1>Admin Panel</h1>

<h2>Add Products</h2>
<form action="/admin/add-products" method="POST" enctype="multipart/form-data">
    <input type="text" name="name" placeholder="Product Name" required><br>
    <input type="number" name="price" placeholder="Price (100 - 700)" required><br>
    <select name="category" required>
        <option value="">Select Category</option>
        <option value="clothes">Clothes</option>
        <option value="shoes">Shoes</option>
        <option value="gadgets">Gadgets</option>
        <option value="accessories">Accessories</option>
    </select><br>
    <input type="file" name="images" multiple required><br>
    <button type="submit">Add Product(s)</button>
</form>

<h2>Customer Messages</h2>
<% messages.forEach((m,i)=>{ %>
<div class="message-box">
  <b><%= m.user %>:</b> <%= m.message %>
  <form action="/admin/reply" method="POST">
    <input type="hidden" name="user" value="<%= m.user %>">
    <input type="text" name="reply" placeholder="Type reply..." required>
    <button type="submit">Reply</button>
  </form>
  <% if(m.reply){ %>
    <p><b>Admin Reply:</b> <%= m.reply %></p>
  <% } %>
</div>
<% }) %>
</body>
</html>
